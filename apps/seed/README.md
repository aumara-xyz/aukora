# apps/seed (scaffold)

Governed inward-out recursion: propose a change, ground it against real files, rehearse in a
sandbox, take an advisory Fu review, refuse on stale/secret/authority, require an AUMLOK
owner-gate, apply only into an isolated sandbox, and capture receipt + lineage. **No
unattended live-repo mutation. No model signs for the owner.**

Layout (to be filled by Worker Two): `src/` orchestration, `demo/` inward-out recursion proof.

Status: **scaffold**. Implementation is Worker Two's lane. Consumes `packages/*`; signing/
custody and live apply stay out of portable packages.

Truth label: DESIGN_ONLY until the recursion proof exists.
